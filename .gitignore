node_modules/
.env
.env.local
.env.*.local
dist/
.next/
coverage/
*.log
.DS_Store
.superpowers/

# Playwright (Tarea 13 del panel web): capturas, trazas e informes de cada
# ejecución local del recorrido — nunca artefactos que se commiteen.
apps/app/test-results/
apps/app/playwright-report/
apps/app/blob-report/

# Vinculación y salida de builds locales de Vercel (`vercel link/build`).
.vercel/

# Tipos y caché que genera Astro en desarrollo/build.
.astro/

# Storybook: salida del build estático y logs locales.
storybook-static/
*storybook.log

# Git worktrees
.worktrees/
